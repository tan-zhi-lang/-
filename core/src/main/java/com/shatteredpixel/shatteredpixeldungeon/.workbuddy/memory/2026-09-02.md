
## 2026-09-02 未来空间器物品消失bug
- 根因：onSelect 存入 Statistics.itemN 的是背包内原对象而非 detach 返回值，同堆叠二次放入导致两槽共用同一对象。
- 已按用户要求将 9 处 isEquipped/doUnequip/detach 分支统一改为 item.detachAll(backpack)，detachAll 会将整个物品从背包移除，引用重复问题随之消除。
- 跨存档刷物品：Statistics.reset() 与 restoreFromBundle() 均未清空 item1~item9 静态字段，静态残留被写入其他存档。
- 修复：Statistics.java reset() 增加 item1~item9=null；restoreFromBundle() 恢复前先置 null 再按 contains 恢复。
- 改为全局互通：item1~9 不再随各存档序列化，改为 未来空间器.saveGlobal()/loadGlobal() 读写全局文件 未来空间器.dat；Dungeon.saveGame 写入、loadGame/reset 读取。物品全局唯一，互通且不可复制。
- 时间回溯功能：Dungeon 新增 回合计数（Hero.act 自增）/回溯上限=3/备份回溯存档()/回溯元数据()/恢复回溯存档()，每20回合 GameScene.update 自动 saveAll+整文件夹快照到 回溯0~2 子目录（含 meta.dat 回合数/层数）。Hero.die 删除存档后可在 WndGame 新增"时间回溯"按钮打开 windows/时间回溯.java 选择快照恢复并 CONTINUE 重载。回合计数随 game.dat 序列化（回合计数x）。注意：死亡时 Rankings 已提交，回溯复活会留下一条死亡记录。
- 时间回溯入口调整：从 WndGame 移除（用户已删），改加到 GameScene.gameOver() 死亡界面（StyledButton 时间回溯按钮，位于 restart 与 menu 之间，menu 下移一位）。
- 时间回溯NPE修复：GamesInProgress.delete() 是缓存 put null（语义=存档已删），恢复存档后 check() 返回缓存 null 导致 InterlevelScene.create NPE；改用 setUnknown() 移除缓存强制重读。
- 时间回溯NPE二次修复：备份回溯存档() 被精简成只写 meta.dat 未复制 game.dat/depth 文件，恢复后存档为空 check() 返回 null；已补回复制逻辑（单文件夹 回溯/）。触发点已由用户改为 buffs/机制/每45回合备份.java（saveAll→备份，顺序正确）。
- 时间回溯NPE第三次修复：备份回溯存档() 再次只剩写 meta.dat（复制逻辑丢失），恢复后文件夹为空 check() 返回 null；已补回复制全部存档文件（排除 回溯/）到 回溯/ 的逻辑。
- 时间回溯一次性加固：备份回溯存档() 先 deleteDir 旧快照再全量复制并校验 game.dat（无则不留脏快照）；恢复回溯存档() 恢复前校验快照含 game.dat、恢复后校验 gameFile 存在；时间回溯窗口 meta 为空时提示并关闭，切换场景前再校验 check()!=null，彻底杜绝 InterlevelScene NPE。
- 回溯失败排查：1层自杀测试提示"快照不存在或已损坏"，原因是旧版备份只写过 meta.dat 的残留快照（无 game.dat）；回溯元数据() 已改为校验快照内 game.dat，无效快照自动 deleteDir 并提示无快照。快照在开局 45 回合后才首次生成。
- 回溯失败根因确认：窗口代码"先 check() 后 setUnknown()"，而死亡时 GamesInProgress.delete() 已在缓存 put null，check() 命中缓存直接返回 null → 永远失败。磁盘证据：game1/game.dat 已被恢复为完整文件。修复：恢复成功后先 setUnknown 清缓存再 check，通过才切场景。
- 新增每2400回合回档：Dungeon 三个方法（备份回溯存档/回溯元数据/恢复回溯存档）参数化快照名，无参重载默认"回溯"；新 buff actors/buffs/机制/每2400回合回档.java（act: saveAll+备份到"回档/"，spend 2400），HeroClass 开局施加；时间回溯窗口改为列出"回溯/回档"两个快照按钮。
- 新增存档导出/导入：utils/存档工具.java 用 java.io 把当前槽位（game.dat+depth*.dat）导出到 用户主目录/SPD存档导出/slot{n}_{时间戳}，导入取最近一次导出覆盖当前槽位并 setUnknown；WndGame 在"备注存档"下新增并排按钮，导出前先 saveAll。
- 导入存档排查：磁盘显示 game1/game.dat 为1字节（死亡占位），导出文件完整——用户活着时导入后角色死亡，deleteGame 把导入内容抹掉（SPD死亡=删档，活着时导入会被当前局覆盖）。导入() 改为原子操作：先复制 .imp.tmp→删旧→改名→校验 game.dat>100字节，失败不破坏现有存档；WndGame 提示改为导入后返回标题点继续。正确用法：死亡界面→菜单→导入存档→返回标题→继续。
- WndGameInProgress 种子行加复制按钮：dungeon_seed 行改为专用布局（数值列宽压缩到0.35），行尾放 14x14 RedButton（Icons.COPY 图标，同 WndTextInput btnCopy），点击把 DungeonSeed.convertToCode(info.seed) 放入剪贴板。
- 导入失败排查：导入() 第二步"清旧文件"把第一步刚生成的 .imp.tmp 一并删光，第三步无可改名，第四步校验 game.dat 长度失败返回 false——且旧存档已被删。修复：第二步跳过 .imp.tmp。导出目录 slot1_20260902_170626 内容完整可恢复。\n- 导入存档改为导入成功后直接 hide 窗口并 Game.switchScene(StartScene.class)，StartScene 重建时 check 重读文件，SaveSlotButton 直接显示；WndGameInProgress 种子复制后弹 WndMessage 提示已复制。\n- 按用户要求撤回最近两项改动：WndGame 导入成功改为恢复原 GLog 提示（不再自动切 StartScene）；WndGameInProgress 移除复制后 WndMessage 弹窗并去掉 GameScene import。保留 setContents 与 .imp.tmp 两个修复。\n- 按用户要求重新启用：WndGame 导入成功 hide 后 Game.switchScene(StartScene.class)，StartScene 重建时 SaveSlotButton 直接 add；WndGameInProgress 复制种子后弹 WndMessage("复制成功")。\n- 导入存档改为 add 至 StartScene：WndGame 导入按钮撤回到原 GLog 提示（不再切换场景）；StartScene 右上角新增"导入存档"按钮，点击调用 存档工具.导入()（curSlot=0 时使用 firstEmpty），成功后 ShatteredPixelDungeon.seamlessResetScene() 重建 StartScene，SaveSlotButton 直接 add。存档工具.导入() 增加 curSlot<=0 自动用 firstEmpty 的兼容逻辑。\n- 导入存档改为 SaveSlotButton 风格：StartScene 在 newGame 后直接 add 一个 importGame（setImport() 标记 importMode=true、name.text("导入存档")）；onClick 中 importMode 分支调用 存档工具.导入()，成功 seamlessResetScene() 重建场景让新存档 SaveSlotButton add 到列表中；layout 中 if (newGame) 已加 !importMode 短路避免被改回"new"。撤回 RedButton import。
- WndGameInProgress 种子复制按钮 Bug 修复：原代码用 GameScene.show(new WndMessage(...))，但 WndGameInProgress 是从 StartScene.scene().add 打开的，GameScene.scene 静态字段为 null 导致 WndMessage 被跳过。改为 Game.scene().add(...) 用当前场景（StartScene）添加窗口；按钮尺寸 14x14 太小不易点击改为 18x18。setContents 路径正确（libGDX 剪贴板），无场景依赖。
- 背包/Bag 容量变动溢出处理：Bag.java 新增 处理容量() 方法，若 items.size() > capacity() 则从尾部取出多余物品 detachAll(this) 后 Dungeon.level.drop 到 hero.pos（sprite().drop() 触发落地动画，可堆叠物品本身只占 1 槽不截断）。WndBag 构造函数在 placeItems 之前对当前 bag 与所有 Bag 调用 一次，确保打开背包时容量被严格遵守。切换 tab 时也会新建 WndBag 重新触发。
