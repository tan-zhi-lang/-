# 2026-09-04
- 修复 9.3/10:50 NPE（Hero.力量() on null）：ui/ItemSlot.java updateText() 中 Weapon/Armor 两分支的 else-if（神力判定）及已鉴定物品区补 Dungeon.hero() 判空；ui/QuickSlotButton.java enableSlot() 补 Dungeon.hero/belongings 判空。触发路径：QuickSlotButton.refresh → item → updateText。
- 修复挖矿无反应：Hero.java 挖矿处理() 优化时误用 Dungeon.相对层数()==3（即 depth==3），而挖矿层 MiningLevel 在 branch1 的 11~14 层，MINE_BOULDER/CRYSTAL 全部被跳过；改为 Dungeon.区域()==3（覆盖 11~15 层），与 actMine/defAction 的区域()==3 门槛一致。
- WndGameInProgress：6 个模式按钮（挑战/炼狱/解压/系统/派对/赛季）改为收集到 ArrayList 后两列自动排版，每行 2 个，奇数时最后一行占半宽靠左。
- WndGame：同上，6 个模式按钮改两列自动排版（复用同一模式），与前后 addButton 全宽按钮的 pos 衔接正常。备注存档、导出存档也并入 modeBtns 两列排版（排在最前，无 hide 语义差异保留原行为）。
- HeroSelectScene 变简单有趣/变困难复杂 图标不刷新：原仅在 onClick 时更新图标；改为按钮 update() 每帧比对状态（解压/系统/派对使用量、挑战/炼狱/赛季数）变化时才重建 icon，窗口关闭后自动同步。
- WndGameInProgress 种子行排版修复：seedCode 文本原上限 WIDTH*0.35 会越过 x=102 的复制按钮；改为按 copyX-GAP-seedX 动态收缩字号，按钮位置用 copyX 变量统一。行高改用 max(txt.height(), 18) 计入复制按钮高度，避免与继续/删除行重叠。
